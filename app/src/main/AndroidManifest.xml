<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uk.co.phartmaster.nav_bar_tut">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuPages.AboutActivity" />
        <activity android:name=".MenuPages.MissionActivity" />
        <activity android:name=".MenuPages.BlogActivity" />
        <activity android:name=".MenuPages.YourTofooActivity" />
        <activity android:name=".MenuPages.ProductsActivity" />
        <activity android:name=".MenuPages.RecipesActivity" />
        <activity android:name=".MenuPages.VideosActivity" />
        <activity android:name=".MenuPages.LegendsActivity" />
        <activity android:name=".MenuPages.StockistsActivity" />
        <activity android:name=".MenuPages.ContactActivity"></activity>
    </application>

</manifest>